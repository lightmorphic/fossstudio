{$DOMAIN} {
	encode zstd gzip
	reverse_proxy 127.0.0.1:{$HTTP_PORT}
}

# The original address forwards to the real one so old links keep working
fossstudio.fosscharlie.uk {
	redir https://app.fossstudio.org{uri} permanent
}
